apiVersion: v1
kind: Service
metadata:
  name: ops-assistant-agent
  labels:
    app.kubernetes.io/name: ops-assistant-agent
    app.kubernetes.io/component: agent-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ops-assistant-agent
    app.kubernetes.io/component: agent-server
  ports:
    - name: http
      port: 80
      targetPort: http
